# URL base de la API del backend. Vite la incrusta en el build en tiempo de
# compilación, así que hay que definirla ANTES de correr "npm run build"
# (o pasarla como build-arg al construir la imagen Docker).
VITE_API_URL=http://localhost:5121/api
